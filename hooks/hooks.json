{
  "description": "JL lifecycle checks for Gemini CLI.",
  "hooks": {
    "BeforeAgent": [
      {
        "hooks": [
          {
            "type": "command",
            "name": "JL task start",
            "command": "node \"${extensionPath}${/}hooks${/}python-launcher.mjs\"",
            "timeout": 5000,
            "description": "Start or continue the JL task lifecycle."
          }
        ]
      }
    ],
    "AfterTool": [
      {
        "hooks": [
          {
            "type": "command",
            "name": "JL task evidence",
            "command": "node \"${extensionPath}${/}hooks${/}python-launcher.mjs\"",
            "timeout": 5000,
            "description": "Record structured consent, knowledge, edit, and build evidence."
          }
        ]
      }
    ],
    "AfterAgent": [
      {
        "hooks": [
          {
            "type": "command",
            "name": "JL task closeout",
            "command": "node \"${extensionPath}${/}hooks${/}python-launcher.mjs\"",
            "timeout": 5000,
            "description": "Require a current evidence-backed knowledge closeout."
          }
        ]
      }
    ]
  }
}
